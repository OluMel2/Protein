<h2>Post detail</h2>
<table class="table">
  <tbody>
    <tr>
      <td>
        <%= link_to @user.name, users_path(@post.user.id) %>
      </td>
      <td><%= image_tag @post.get_image, width: "100×100" %></td>
      <td><%= link_to @post.product_name, post_path(@post.id) %></td>
      <td><%= @post.tast %></td>
      <td><%= @post.price%></td>
      <td><%= @post.net%></td>
      <td><%= @post.serving_size %></td>
      <td><%= @post.calorie %></td>
      <td><%= @post.protein %></td>
      <td><%= @post.description %></td>
      <td>
        <% if @post.user == current_user %>
        <%= link_to "Edit", posts_information_edit_path(@post.id), class: "btn btn-sm btn-success" %>
        <% end %>
      </td>
      <td>
        <% if @post.user == current_user %>
        <%= link_to "Destroy", post_path(@post), class: "btn btn-sm btn-danger", method: :delete %>
        <% end %>
      </td>
    </tr>
  </tbody>
</table>